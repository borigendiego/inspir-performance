import React from 'react';
import './home.scss';

const Home = () => {
    return(
        <div className={'home-wrapper'}>
            <h1 className={'home-title-one'}>STRENGTH YOUR BUNDARIES</h1>
            <h3 className={'home-title-two'}>YOUR MIND HOLDS THE KEY</h3>
        </div>
    )
};

export default Home;