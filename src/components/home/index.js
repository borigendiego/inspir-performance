import React from 'react';
import './home.scss';

const Home = () => {
    return(
        <div className={'home-wrapper'} id={'home'}>
            <div className={'home-overlay'}/>
            <h1 className={'home-title-one'}>STRENGTH YOUR BUNDARIES</h1>
            <h5 className={'home-title-two'}>YOUR MIND HOLDS THE KEY</h5>
        </div>
    )
};

export default Home;